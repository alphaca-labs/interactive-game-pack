# Stage 02 / Item 05 — `pixel-pour` 구현 완료

담당: 최이상 · 완료일: 2026-08-06 · 구현 경로: `games/game-05/`

## 구현 결과

- 16×16 픽셀 작품 2종(김 나는 머그컵 / 체리 컵케이크), 작품당 108셀·15배치·6색/6문양을 구현했다.
- 포인터 드래그·탭 선택·키보드(`Enter`, 방향키, `1–6`, `Esc`)가 동일 판정 로직을 사용한다.
- 전체/레인별 진행률, 콤보·반응 보너스·오답 감점, 38초 타이머, 0초 포인터 판정 유예, 성공/실패·재시작 도안 교대를 구현했다.
- Canvas 파티클·완성 스윕, Web Audio 합성음, 음소거 저장, 탭 비활성 일시정지, `prefers-reduced-motion`, 라이브 리전과 44px 최소 타깃을 포함했다.
- 외부 이미지·폰트·라이브러리 없이 `index.html`, `style.css`, `script.js` 세 파일만으로 독립 실행된다.

## 검증

- `node --check games/game-05/script.js` 통과.
- 브라우저 `?test=1`: 도안 2종 216셀, 30배치 불변식 통과; 콘솔 예외 0건.
- Chrome CDP 320×568: `scrollWidth=320`, `scrollHeight=568`, 드래그 정답·키보드 오답·100% 완주·결과·재시작 통과.
- `prefers-reduced-motion: reduce`에서도 키보드 정답과 진행률 갱신 통과.
- 네트워크는 해당 폴더의 HTML/CSS/JS와 인라인 `data:` 리소스만 사용하며 외부 요청은 없다.
- 전체 원본 57,982B, gzip 합계 17,013B로 180KB 기준 이내다.

## 범위 준수

공용 진입점과 다른 게임 디렉터리는 수정하지 않았고, 하위 작업 지시에 따라 Git commit·push·배포는 수행하지 않았다.
