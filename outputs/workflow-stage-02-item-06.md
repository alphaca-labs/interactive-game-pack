# Stage 02 / Item 06 — 러시 카운터 구현 완료

담당: 최이상 · 완료일: 2026-08-06

## 구현 결과

- 대상: `games/game-06/` — 러시 카운터(Rush Counter), Type D 시뮬레이션
- 독립 산출물: `index.html`, `style.css`, `script.js`
- 코어 루프: 버거·감자·음료 조리 → 완료품 보관 → 활성 주문 서빙 → 계산대 현금 수거 → 수거액 160코인 달성
- 게임 규칙: 45초, 손님 3명 이탈 시 실패, 조리 완료 유예·폐기/청소, 음식별 보관 2개, 인내·속도·연속 보너스, 미수거 현금 분리 원장
- 입력: 포인터/터치, 조리대 `1/2/3`, 서빙 `Space`, 현금 `C/4`, 일시정지 `P/Esc`, 결과 재시작 `R`
- 품질: 320px 세로·가로 2열 반응형, 44px 이상 주요 조작, 키보드 포커스, live region, 모달 포커스 순환, reduced motion/고대비 대응
- 에셋: 외부 이미지·폰트·음원·CDN 없이 CSS 도형과 Web Audio 합성음만 사용

## 검증

- `node --check games/game-06/script.js` 통과
- 정적 서버에서 `index.html`, `style.css`, `script.js` 모두 HTTP 200 응답 및 파일 크기 일치
- 외부 `http(s)` 및 CSS `url()` 참조 0건
- `?test=1&seed=6` 자기 테스트 통과: 시드 난수 재현, 주문 크기·중복 금지, 재고 판정, 지급액/연속 상한
- DOM 모의 통합 테스트 통과: 첫 주문의 버거·음료 조리/보관 → 서빙 1건 → 미수거 34코인 생성 → 현금 수거 후 보유 34·미수거 0
- 인앱 브라우저 자동화는 현재 호스트에 연결 가능한 브라우저 인스턴스가 없어 실행하지 못했다. 정적 서버와 로직/DOM 통합 검증으로 대체했으며 실제 브라우저 시각 회귀는 통합 단계의 잔여 확인 사항이다.

## 범위 준수

- 구현은 `games/game-06/`에만 추가했다.
- 공용 진입점과 다른 게임 디렉터리는 수정하지 않았다.
- 이 병렬 하위 작업 지시에 따라 Git commit, push, PR, GitHub Pages 배포는 수행하지 않았다.
