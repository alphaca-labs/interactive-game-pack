# Stage 02 / Item 08 — Dish Collect 구현 완료

담당: 최이상 · 완료일: 2026-08-06

## 구현 결과

- 전용 경로 `games/game-08/`에 빌드 과정과 외부 의존성이 없는 HTML/CSS/JavaScript 게임을 구현했다.
- 접시별 레시피 4종, 6열×8행 48칸 중 36타일, 요구 재료 20개 우선 생성, 방해 타일 16개, 정답/소프트 미스/하드 미스, 40초 타이머, 콤보, 점수, 6초 힌트, 성공/시간 실패, 재시작을 완성했다.
- 14종 재료 그래픽은 모두 자체 인라인 SVG이며, 하늘·접시·체크 보드·파티클은 CSS와 절차형 효과로 구성했다. 외부 이미지·폰트·음원·라이브러리 요청은 없다.
- 마우스·터치 외에 roving tabindex 격자, 방향키, Enter/Space, H/M/R 단축키, live region, 여유 모드, 음소거, 축소 동작, 강제 색상 대응을 포함했다.

## 생성 파일

- `games/game-08/index.html`
- `games/game-08/style.css`
- `games/game-08/script.js`

## 검증 결과

- `node --check games/game-08/script.js`: 통과
- `?test=1` 내장 검사: 100개 seed 모두 타일 36개, 좌표 중복 없음, 요구량 20개, 타입별 공급량 불변식, 44px 이상 타깃, 판정 분기 통과
- 390×844 실제 플레이: 4개 레시피 연속 완주 → 성공 `4/4` → 결과 모달 → 새 seed 재시작 통과, 런타임 예외 0건
- 오답 경계: 소프트 미스 -1초, 하드 미스 -3초, 두 경우 모두 타일 유지, 같은 타일 250ms 연타 페널티 중복 방지 통과
- 힌트: 6초 뒤 활성화, 현재 정답 타일 1개 굵은 표시, 최종 점수 -75 반영 통과
- 키보드: 방향키 포커스 이동, 수집 후 포커스 복구, 결과 모달 포커스 순환 통과
- 반응형: 320×568 최소 타일 47.3px, 390×844 가로 초과 0px, 844×390 최소 타일 45.1px 및 필수 조작 스크롤 접근 통과
- 오프라인: `file://` 직접 실행에서 게임 시작·36타일 렌더링·내장 검사 통과, 외부 요청 0건, 런타임 예외 0건

## 범위 준수

이번 하위 작업은 항목 전용 디렉터리와 지정 결과 보고서만 작성했다. 공용 진입점, 다른 게임, Git commit/push, GitHub Pages 배포는 상위 통합 단계의 소유 범위이므로 변경하지 않았다.
